# Runtime dependencies for hedgefund-ai
# Keep in sync with pyproject.toml [project.dependencies]

# Data & numerics
pandas>=2.1
# numpy MUST stay <2: the cross-encoder reranker (torch) and onnxruntime
# native layers, plus the pure-Python vector store, are unstable on the
# numpy 2.x C ABI on this Windows/py3.12 environment. A 2.x install was
# the proximate cause of native ("Python has stopped working") crashes.
numpy>=1.26,<2
requests>=2.31
yfinance>=0.2.40
sqlalchemy>=2.0
python-dotenv>=1.0

# Modeling
scikit-learn>=1.4

# UI
streamlit>=1.36
plotly>=5.20

# PDF report generation
reportlab>=4.0

# RAG — vector store + retrieval
# The vector store is a pure-Python NumPy implementation (rag/vector_store.py);
# chromadb was removed because its native chroma-hnswlib layer segfaults on
# write on this environment and a non-crashing version needs a C++ compiler
# to build. sentence-transformers is the fallback embedder (when Ollama /
# nomic-embed-text is unreachable) and also powers the cross-encoder
# reranker. rank-bm25 backs the BM25 half of the hybrid retriever.
sentence-transformers>=2.7
rank-bm25>=0.2.2

# Dev / test
pytest>=8.0
pytest-mock>=3.12