# Runtime dependencies for hedgefund-ai
# Keep in sync with pyproject.toml [project.dependencies]

# Data & numerics
pandas>=2.1
numpy>=1.26
requests>=2.31
yfinance>=0.2.40
sqlalchemy>=2.0
python-dotenv>=1.0

# Modeling
scikit-learn>=1.4

# UI
streamlit>=1.36
plotly>=5.20

# PDF report generation
reportlab>=4.0

# RAG — vector store + retrieval
# chromadb is the persistent vector DB. sentence-transformers is the
# fallback embedder (when Ollama / nomic-embed-text is unreachable) and
# also powers the cross-encoder reranker. rank-bm25 backs the BM25
# half of the hybrid retriever.
chromadb>=0.5.0
sentence-transformers>=2.7
rank-bm25>=0.2.2

# Dev / test
pytest>=8.0
pytest-mock>=3.12