"""UI layer for the hedge-fund AI research system.
 
Contains the Streamlit dashboard (`app.py`) that surfaces portfolio state,
per-ticker agent context, risk/thesis output, and macro context on top of
the `data/`, `agent/`, and `quant/` packages.
 
Run with:
    streamlit run ui/app.py
"""
 
__all__ = []
 